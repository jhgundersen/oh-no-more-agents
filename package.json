{
  "name": "oh-no-more-agents",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Autonomous agents attempting Lemmings-like levels, with a global rescue counter.",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev:agent": "wrangler dev --local --port 8788",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply oh-no-more-agents --local",
    "db:remote": "wrangler d1 migrations apply oh-no-more-agents --remote",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/counter.js && node --check public/agents/Sim.js && node --check public/agents/Draw.js && node --check public/agents/Palette.js && node --check public/agents/web.js && node tools/check-core-refs.js && node tools/stamp-assets.js --check && npm test",
    "sim": "node tools/simcheck.js",
    "stamp": "node tools/stamp-assets.js",
    "sync": "node tools/sync-copies.js",
    "sync:check": "node tools/sync-copies.js --check"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
